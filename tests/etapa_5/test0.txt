int teste1, teste2, teste3;

(float value1, bool b, int value2, int value3, int value4) >= bool! function1
{
    int new_value;

    if (b == value1)
    {
        new_value = value1 * value2;
        b = 8;
    }
    else
    {
        int sla;
        new_value = value1 / value2;
    };

    //int sla; // isso daqui, de acordo com a especificação do professor, deveria dar erro, arrumar dps
    bool b1, b2, b3, b4, b5;

    b = b1 + b2 + (b3 * (b4 / b5));
    
    {};
    {{{{};};};};

    function1(b1, b2, b3, b4);

    while(b) {};
    while(b==b) {{};};

    if (value2){ value1 = value2; };

    return new_value;
}

() >= int! function2
{
    float number;

    teste1 = teste2;
    teste2 = teste3;
}

int a, b, c, d, e;