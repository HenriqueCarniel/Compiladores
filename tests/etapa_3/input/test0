int teste1, teste2, teste3;

(float value1, bool b, int value2) >= bool! function
{
    if (b)
    {
        new_value = value1 * value2;
    }
    else
    {
        new_value = value1 / value2;
    };

    b = b1 + b2 + (b3 * (b4 / b5));
    int teste1;
    
    {};
    {{{{};};};};

    function(b1, b2, b3, b4);

    while(b) {};
    while(b==b) {{};};

    b3 = function1() + function2();    


    if (value2){ value1 = value2; };

    return new_value;
}
